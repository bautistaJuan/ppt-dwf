{
  "name": "ppt",
  "version": "1.0.0",
  "description": "Piedra, Papel o Tijera",
  "scripts": {
    "dev": " parcel src/index.html",
    "buildGh": "parcel build ./src/index.html --dist-dir build --public-url /dwf-m5-desafio-final",
    "build": "parcel build ./src/index.html",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "parcel": "^2.10.0",
    "typescript": "^5.2.2"
  }
}
